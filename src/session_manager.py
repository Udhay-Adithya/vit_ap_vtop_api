import requests

# Initialize a requests session object for making HTTP requests.
# The session object maintains connections and provides a persistent cookie storage.
requests_session = requests.Session()
